int	take(char *str)
{
	int a;
	int b;
	
while (str[i] != '\0')
{
	if (str[i] == '+')
		return (a + b);
	if (str[i] == '-')
		return (a - b);
	if (str[i] == '*')
		return (a * b);
	if (str[i] == '/')
		return (a / b);
	
}
}

